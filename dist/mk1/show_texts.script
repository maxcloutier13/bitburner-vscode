tail();
var directory = "/mk1/scan/";
disableLog("ALL");

//Iterating trough all files
var textfiles = ls(getHostname(), "/scan/");
print("-- Showing " + textfiles.length + " files");
for (var z in textfiles) {
	print("File:" + textfiles[z] + " - " + read(textfiles[z]))
}